services:
  mariadb:
    image: mariadb:latest
    container_name: to_do_mariadb
    environment:
      MARIADB_DATABASE: semana3db
      MARIADB_ALLOW_EMPTY_ROOT_PASSWORD: 1
    ports:
      - "3306:3306"
